#include "Collider.h"
/*
Collider::Collider(SDL_Rect rectangle, COLLIDER_TYPE type, Module* callback) :
	rect(rectangle),
	type(type),
	callback(callback),
	to_delete(false),
	isCollising(false)
{}

void Collider::SetPos(fPoint pos)
{
	rect.x = pos.x;
	rect.y = pos.y;
}

bool Collider::CheckCollision(SDL_Rect r) const
{
	return SDL_HasIntersection(&rect, &r);
}
*/